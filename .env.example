# Copy to ~/.obsidian_android_sync_state/.env and customize (setup seeds this on first run).
# Vault path must be accessible to Android/Obsidian.

OBSIDIAN_DIR_PATH=/storage/emulated/0/repos/Obsidian

# Git object storage after worktree-fix: ~/.obsidian_android_sync_state/git-repos (automatic)
